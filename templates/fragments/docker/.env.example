# Copy to .env and edit. docker-compose reads .env automatically.

PORT=8080
HOST=0.0.0.0
LOG_LEVEL=info

# Replace with `openssl rand -hex 32` output before deploying.
SESSION_SECRET=dev-only-change-me-in-prod
