---
title: Wun
description: Clojure-first cross-platform server-driven UI framework.
template: splash
hero:
  tagline: Server-driven UI for native apps and the web. One vocabulary, one wire, three platforms — rendering in lock-step.
  image:
    dark:  ../../assets/hero-loop-dark.svg
    light: ../../assets/hero-loop-light.svg
  actions:
    - text: Get started
      link: /getting-started/install/
      icon: right-arrow
      variant: primary
    - text: Why Wun
      link: /getting-started/why-wun/
      icon: open-book
      variant: minimal
    - text: View on GitHub
      link: https://github.com/Holy-Coders/wun
      icon: external
---

import { CardGrid, LinkCard } from '@astrojs/starlight/components';
import PlatformStrip from '../../components/PlatformStrip.astro';

## The same screen, on every platform

<div class="wun-grid-bg">
  <PlatformStrip />
</div>

The server holds state. Patches stream over a single SSE channel.
Every client renders natively from the same tree. Tap a button, the
intent flies back, the server morphs state, and every connected
client sees the new tree on the next patch.

## What you get

<CardGrid>
  <LinkCard
    title="Three macros, total"
    href="/concepts/components/"
    description="defcomponent, defscreen, definent are the entire authoring API. Framework code uses the same APIs as user code — there is no privileged path."
  />
  <LinkCard
    title="One wire format"
    href="/concepts/wire-format/"
    description="Transit-json or JSON envelopes carry patches, intents, screen metadata, and connection state. The same shape lands on the web, in SwiftUI, and in Compose."
  />
  <LinkCard
    title="Optimistic by default"
    href="/concepts/intents/"
    description="The :morph fn runs identically on server (authoritative) and client (predictive). Intents reconcile via UUID; the UI updates before the round-trip completes."
  />
  <LinkCard
    title="WebFrame fallback"
    href="/concepts/capabilities/"
    description="Any component the native client lacks collapses to a server-rendered HTML frame at the smallest containing subtree. New components ship without breaking older clients."
  />
</CardGrid>

## Inspired by

[Phoenix LiveView](https://hexdocs.pm/phoenix_live_view) for the
stateful-socket / optimistic-prediction model, [Hotwire
Turbo](https://turbo.hotwired.dev) for transport patterns + head
merging, and [SDUI](https://martinfowler.com/articles/server-driven-ui.html)
in general for the server-as-source-of-truth posture. The Clojure
side leans on data-as-API + macros to keep the surface tight.

## Status

The framework is a working spike — see the
[changelog](https://github.com/Holy-Coders/wun/commits/master) on
GitHub for what's landed. Browse the docs sidebar for the concepts
and the CLI reference.
